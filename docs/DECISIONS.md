# Implementation decisions

2026-09-06: The current owner request supersedes the earlier deployment wording in PRODUCT.md and DESIGN.md for this delivery: build a local single-user application. GCP remains a future deployment direction.

The owner confirmed that "no fallback" removes legacy/v1 compatibility, while explicit controller-provider failover remains supported and receipted. Signal-only inspection is a configured capability with visible limitations.

The two routes are a cinematic landing/project entrance and the workspace. Fit, Record and Review are modes of the workspace. The authored four-reference synthesis and palette remain authoritative.
